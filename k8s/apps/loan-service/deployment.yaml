apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-service
  labels:
    app.kubernetes.io/name: loan-service
    app.kubernetes.io/part-of: originate
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: loan-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: loan-service
        app.kubernetes.io/part-of: originate
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: loan-service
          image: ghcr.io/chiewhui1113/originate-loan-service:sha-836fc774dfe06fe61f934909f3dc86396e38aa1c
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8082
          env:
            - name: SERVER_PORT
              value: "8082"
            - name: CUSTOMER_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: loan-service-config
                  key: CUSTOMER_SERVICE_URL
            - name: PRODUCT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: loan-service-config
                  key: PRODUCT_SERVICE_URL
            - name: DECISION_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: loan-service-config
                  key: DECISION_SERVICE_URL
            - name: APP_CORS_ALLOWED_ORIGIN
              valueFrom:
                configMapKeyRef:
                  name: loan-service-config
                  key: CORS_ALLOWED_ORIGIN
            - name: DEMO_ERROR_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: loan-service-config
                  key: DEMO_ERROR_ENABLED
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 2
            failureThreshold: 4
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 60
            timeoutSeconds: 3
            failureThreshold: 2
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: loan-service
  labels:
    app.kubernetes.io/name: loan-service
    app.kubernetes.io/part-of: originate
spec:
  selector:
    app.kubernetes.io/name: loan-service
  ports:
    - name: http
      port: 8082
      targetPort: http
